export const JWT_AUTH_TOKEN_SECRET = 'this is top secret 123';
export const JWT_REFRESH_TOKEN_SECRET = 'this is top 321 secret';

// Generate a secure random secret with the following command:
// node -e "console.log(require('crypto').randomBytes(256).toString('base64'));"
